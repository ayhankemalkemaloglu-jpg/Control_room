# Hermes backend base URL (no trailing slash)
VITE_API_BASE=http://localhost:4000
# AUTH_TOKEN from the backend. Optional here — you can also paste it in the UI
# (stored in localStorage), which keeps the secret out of the built bundle.
VITE_AUTH_TOKEN=
