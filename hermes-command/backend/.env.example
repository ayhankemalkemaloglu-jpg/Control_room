# Hermes backend configuration
PORT=4000
BRIEFING_INTERVAL_MS=30000

# Allowed browser origins (comma-separated). Leave unset to use the built-in
# defaults: http://localhost:3000 and http://hermes.local (with/without :3000).
# CLIENT_ORIGIN=http://localhost:3000,http://hermes.local:3000

# Phase 2 — Telegram (unused in the Phase 1 skeleton)
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHANNEL_ID=
